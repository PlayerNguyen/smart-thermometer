{
  "name": "smart-thermometer-server",
  "version": "1.0.0",
  "main": "src/index.js",
  "private": "true",
  "devDependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.16"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon -r dotenv/config src/index.js",
    "deploy": "cross-env NODE_ENV=production node -r dotenv/config src/index.js "
  }
}
